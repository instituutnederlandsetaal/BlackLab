# Example of convert and tag plugin

This is an example of the implementation of two plugins that allow you to run a document conversion (e.g. from Word 
to TEI) and tagging step prior to indexing. See [plugins](/development/customization/) for more information.

