import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"The Example Application","description":"","frontmatter":{},"headers":[],"relativePath":"guide/example-application.md","filePath":"guide/_example-application.md","lastUpdated":1749639304000}');
const _sfc_main = { name: "guide/example-application.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="the-example-application" tabindex="-1">The Example Application <a class="header-anchor" href="#the-example-application" aria-label="Permalink to &quot;The Example Application&quot;">​</a></h1> <div class="tip custom-block"><p class="custom-block-title">NOTE</p> <p>For an even simpler example, see <a href="/guide/getting-started.html">Getting started</a>.</p></div> <p>Included in the library is a simple application that demonstrates indexing and searching all in one. Note that you would normally have two separate applications doing these two tasks.</p> <p>The example application can be found in the nl.inl.blacklab.example package. The main class is Example. The application is fairly well-commented, but we&#39;ll give a quick overview here as well.</p> <p>The main function consists of an indexing and a searching part. Indexing is done by creating a temporary directory, instantiating the Indexer class with the DocIndexerExample class as a parameter (see below), then looping over an array and indexing its values. Normally you would use one of the Indexer.index() functions to index a file or a whole directory tree, but indexing from an in-memory array simplifies the example.</p> <p>The DocIndexerExample class that we passed to the Indexer constructor defines how to parse and index a specific input format. There are also DocIndexer classes for TEI P4, FoLiA, Alto, and so on. The example uses a very simple made-up XML structure. The DocIndexerExample constructor sets up the index structure and adds handlers for different elements. The standard index structure has a single annotated field, &quot;contents&quot;. Annotated fields are like Lucene fields, but with multiple &quot;annotations&quot; per token position, so we can store lemma, part of speech, or any other annotations along with the word itself. Added to the annotated field are several annotations (&quot;pos&quot; for part of speech, &quot;lemma&quot; for lemma). Lemma is added with a different sensitivity setting than pos. Because of this, we can search case-sensitively as well as case-insensitively in lemma, but not in pos.</p> <p>You should be able to figure out the rest of the DocIndexerExample class reading the included comments. However, it is very important to note that you must call addValue for all annotations for each token you encounter. If you don&#39;t call these methods exactly once for annotation for each token, the token positions start getting out of synch and searching will produce the wrong results.</p> <p>(Have a look at the other DocIndexer classes for a bit more advanced indexing, including indexing sentence tags and metadata)</p> <p>If you view a BlackLab Lucene index in the Luke index viewer (you would need to set a breakpoint to do this in the example application, because it cleans up the temporary directory at the end), you can see how annotations work: they are stored as separate fields in the Lucene index, and combined when searching.</p> <p>The search part of the main function should be pretty self-explanatory, and hopefully demonstrates how easy it is to use BlackLab. Two things perhaps require some clarification: the statement &quot;hits.findConcordances()&quot; uses the forward index (a special way of storing the originally indexed content that allows us to quickly assemble specific parts of documents) to build XML context snippet for each of the hits found, so they can be displayed. The following loop uses the utility method xmlToPlainText to strip out any XML tags in the snippet.</p> <p>That hopefully explains the basics of indexing and searching with BlackLab. If part of this is still unclear, or you have additional questions, don&#39;t hesitate to <a href="/guide/about.html#contact-us">contact us</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/_example-application.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _exampleApplication = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _exampleApplication as default
};
