# Node libraries
/node_modules

# Various files not needed inside container
/.gitignore
/.dockerignore
/Dockerfile
/*.md

# These will be bind mounted (BUT DOESN'T WORK, LOOK INTO THIS)
#./data
#./start-with-test-data.sh
