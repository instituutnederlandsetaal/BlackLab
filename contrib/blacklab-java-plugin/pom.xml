<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.example.something</groupId>
    <artifactId>blacklab-java-plugin</artifactId>
    <version>1.0</version>
    <packaging>jar</packaging>

    <name>Template for a BlackLab Java plugin</name>
    <description>
        Example of how you can set up a Java plugin for BlackLab.
        You can build this Maven project using `mvn package`.
        The resulting JAR file can be placed in the `plugins` directory of your BlackLab installation.
        See https://blacklab.ivdnt.org/development/customization/ for more information.
    </description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>17</maven.compiler.release>
    </properties>

    <dependencies>
        <dependency>
            <groupId>nl.inl.blacklab</groupId>
            <artifactId>blacklab</artifactId>
            <version>5.0.0-SNAPSHOT</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.0</version>
            </plugin>
        </plugins>
    </build>

</project>
